'use strict';
module.exports = function(sequelize, DataTypes) {
  var Pastern = sequelize.define('Pastern', {
    name: DataTypes.TEXT
  }, {
    classMethods: {
      associate: function(models) {
        // associations can be defined here
      }
    }
  });
  return Pastern;
};